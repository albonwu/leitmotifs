import React, { useState } from "react";
import FlashCard from "./FlashCard";

type GalleryProps = {
  cards: any;
};
const Gallery: React.FC<GalleryProps> = ({ cards }: GalleryProps) => {
    
  return <></>;
};

export default Gallery;
